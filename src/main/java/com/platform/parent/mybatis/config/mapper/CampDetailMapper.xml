<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.parent.mybatis.dao.CampDetailMapper">
    <insert id="add" parameterType="com.platform.parent.mybatis.bean.CampDetail">
        INSERT INTO camp_detail (cd_id, announcement) VALUES (#{id}, #{announcement})
    </insert>
    <update id="update" parameterType="com.platform.parent.mybatis.bean.CampDetail">
        UPDATE camp_detail SET announcement = #{announcement} WHERE cd_id = #{id}
    </update>
    <delete id="deleteByIds" parameterType="java.lang.String">
        DELETE FROM camp_detail WHERE cd_id IN
        <foreach collection="array" item="idItem" open="(" separator="," close=")">
            #{idItem}
        </foreach>
    </delete>
</mapper>